# SeatView 프로젝트 개발 규칙 (Project Rules)

이 프로젝트에서는 다음의 사용자 정의 저장 및 깃허브 반영 규칙을 엄격히 준수합니다.

## 1. 저장 명령어 규칙
* 사용자가 **"저장해줘"** 혹은 **"이건확정해줘"**라고 명시적으로 말할 때만 작업 내용을 로컬 파일시스템에 저장/확정합니다.
* 사용자가 "만들어줘", "구현해봐" 등 구현을 지시할 때는 기존에 저장된 코드를 기반으로 HTML/CSS/JS 파일에 반영하여 업데이트합니다.

## 2. 깃허브(GitHub) 반영 규칙 (CRITICAL / 필수정책)
* 사용자가 **"깃허브에 올려줘"** 또는 **"깃허브에 반영해"** 라는 정확한 명령을 내리지 않는 한, **어떠한 경우에도 깃허브에 푸시(`git push`)하거나 커밋(`git commit`)을 진행하지 않습니다.**
* 깃허브 배포(Netlify 자동 배포 포함)를 확인하고 싶다는 의사를 표현하더라도, 명시적 반영 명령어가 떨어지기 전까지는 오직 **로컬 작업 폴더 내 파일에만 코드를 저장**합니다.
* 이 규칙은 사용하는 AI 코딩 도구(Antigravity, Claude Code 등)에 관계없이 이 프로젝트의 공통 정책입니다.

## 3. 코드 수정 후 자체 검증 규칙 (TEST VERIFICATION)
* 코드나 파일을 수정하고 난 후에는, **반드시 기존 화면이 올바르게 로드되고 오동작하지 않는지 사전에 검증**한 후 사용자에게 답변합니다.
* 특히 JavaScript 수정 후 세미콜론 누락, 이중 상수 선언(Duplicate Declaration), 템플릿 리터럴 괄호 누락 등으로 페이지 전체가 로드되지 않는 현상이 없는지 로컬 브라우저 구동 등을 통해 철저하게 검증해야 합니다.

## 4. 이벤트 핸들러 및 입력창 검증 규칙
* HTML의 `oninput`, `onchange`, `onclick` 등 인라인 이벤트 핸들러에 바인딩된 JavaScript 함수(예: `autoResizeTextarea` 등)가 실제 코드 상에 올바르게 정의되어 있는지 반드시 확인해야 합니다.
* 텍스트 입력창 등에 글자를 타이핑하거나 파일을 직접 선택해 보는 등 실제 수동 조작을 거쳐 개발자 도구 콘솔에 빨간색 에러 로그가 단 하나도 찍히지 않는 것을 눈으로 확인한 뒤 최종 보고해야 합니다.

## 5. 소스 수정 작업 절차 (필수정책)
소스 코드를 수정할 때는 반드시 아래 순서를 지킵니다.
1. **계획**: 어떤 방식으로 수정할지 먼저 생각하고 정리합니다.
2. **영향도 검토**: 이 수정이 다른 기능/화면/파일에 영향을 주지 않는지 검토합니다 (예: 같은 함수/변수/CSS 클래스를 다른 곳에서도 쓰고 있는지 확인).
3. **수정**: 검토가 끝난 뒤에 실제 코드를 수정합니다.
4. **테스트**: 최소한 해당 기능에 대해서는 로컬 환경(http://localhost:8080/)에서 직접 동작을 확인합니다.
5. **보고**: 위 절차가 모두 끝난 뒤에만 사용자에게 "완료했다"고 보고합니다.

