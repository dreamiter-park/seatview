# SeatView 작업 세션 요약 (2026-08-07 ~ 08-08)

이 문서는 컨텍스트 압축/세션 재시작 시 빠르게 맥락을 되찾기 위한 요약입니다.
**아직 깃허브에 반영 안 된 로컬 전용 변경사항이 많습니다** ("깃허브에 올려줘"라고 명시적으로 말할 때까지 push 금지 — project_rules.md 참고).

## 보안
- `admin.html`의 service_role 키를 하드코딩 → **비밀번호로 암호화해 localStorage에 저장**하는 방식으로 전환 (Web Crypto PBKDF2+AES-GCM). 최초 1회 키+비밀번호 설정, 이후엔 비밀번호만 입력.
- admin 클라이언트에 `persistSession:false` 등 옵션 추가 — index.html과 같은 origin이라 로그인 세션을 잘못 물려받는 문제 방지.
- `seat_reviews` INSERT 정책이 `WITH CHECK(true)`였던 것 확인 → `auth.uid()=user_id`로 강화하는 SQL 제공(적용 여부는 사용자 확인 필요할 수 있음).

## 데이터 정합성 (가장 중요한 구조적 수정)
- `seat_reviews.baseball_seat_id`: 원래 여러 곳에서 제각각 다른 문자열 조합으로 만들던 것 → **`baseball_seats.id`를 참조하는 진짜 정수 FK로 전환**. `openSeatDetail()`이 이제 실제 seat→block→stadium을 조회해서 정확한 이름을 표시.
- `STADIUMS_DB`에 `map_image_url` 필드가 누락되어 있었음 → 추가함. 잠실 DB 값(`assets/stadium_01.png`)이 실제 파일 위치(`stadiums/stadium_01.png`)와 달라서 404 나던 것 발견, SQL로 수정 요청함.
- 구장 슬러그 매핑에서 수원↔인천이 뒤바뀌어 있던 것 발견 후 수정.

## 마이페이지 / 시야 상세
- 로그인/로그아웃을 햄버거 메뉴에서 **마이페이지 화면 안으로 이동**. 로그아웃 상태에서도 마이페이지 진입 가능(로그인 프롬프트 표시).
- 로그아웃 시 항상 홈으로 이동하도록 수정 (기존엔 안 됐음).
- 티켓 카드 클릭 → 좌석 상세 모달 열림(`openSeatDetail(seatId, {ownReviewsOnly:true})`) — 본인이 쓴 리뷰만 필터링해서 보여줌.
- 상세 모달 하단 버튼: 마이페이지에서 열었을 때는 **[기록 수정] [기록 삭제]**로 교체 (원래 있던 [1:1비교담기][제보하기]는 숨김). **등록 후 3일 이내만 수정/삭제 가능** (`isWithinEditWindow()`).
- 수정 기능은 기존 제보 폼을 재사용, 제출 시 INSERT 대신 UPDATE (`state.editingReviewId`로 분기).
- 삭제 버튼을 카드 안(작은 아이콘, 오터치 위험)에서 상세 모달로 이동 — 모바일 터치 타겟 문제 해결.
- 티켓 카드에 등록일 표시(우측 하단), 여러 장 업로드 시 "🖼N" 배지 표시.

## 홈 화면 / 네비게이션
- 상단 햄버거 메뉴 → **하단 GNB 4탭**(홈/시야조회·등록/1:1시야비교/마이페이지)으로 교체. 하이브리드 앱 전환을 염두에 둔 결정.
- "시야조회/등록" 탭은 현재 "야구장 시야 탐색"과 동일 화면 연결 (추후 OCR 자동등록 기능 생기면 분리 예정).
- 카테고리 카드 클릭 로직을 `is_active` 대신 `badge_text === "TO BE"` 기준으로 통일.
- 검색창: "구장명 구역번호" 파싱해서 자동 이동+좌석 선택 구현 (예: "고척 111").
- 카카오 애드핏 배너: 홈 화면 스크롤 콘텐츠 최하단에 배치(고정 아님, GNB랑 겹치지 않게).
- 구장 목록에 상품 광고 슬롯 1칸 삽입 (대구 삼성 라이온즈 자리 앞, `display_order` 기준으로 동적 위치 계산).

## 1:1 시야 비교
- 비교 카드가 실제 여러 장의 사진+리뷰를 캐러셀로 보여주도록 수정 (기존엔 대표 이미지 1장만).
- DB에서 안 쓰는 목업 태그("승리/패배 기운" 등) 제거.
- 세션 관리: **마지막 활동 후 30분** 지나면 자동 초기화 (localStorage + timestamp 체크).
- "비우기" 버튼: 헤더에 넣었다가 사용자 피드백으로 장바구니 스타일 콘텐츠 영역 툴바로 재배치.
- 최대 비교 개수: **2개 유지** (기능명이 "1:1"이라 그대로).

## 기타 정책/제약
- 관람평/코멘트: **최대 120자**로 입력 제한 (4줄 기준 실측). 화면 표시도 120자+"…"로 이중 안전장치.
- 로그인 관련 native `confirm()`/`alert()` → 커스텀 디자인 모달로 전부 교체.
- 스토리지: 시야 사진은 Storage 버킷 없이 **WebP 압축된 base64를 DB 컬럼에 직접 저장** (추후 Storage 이전 고려 가능, 아직 미결정).
- 구장 지도 이미지(`map_image_url`)는 Storage가 아니라 **프로젝트 로컬 파일 경로**를 DB에 저장하는 방식 (`assets/`, `stadiums/` 폴더).

## 대기 중 (아직 미착수)
- **사직구장 7~8행 사이 통로 표현**: `baseball_blocks`에 nullable 컬럼(예: `aisle_after_row`) 추가해서 순수 시각적 여백만 주는 방식으로 논의됨. 행 번호 계산 로직(`getRowLabel`)은 grid_y 기반 순차 계산이라 손대면 안 됨. 사용자가 사직구장 데이터 입력 끝나면 다시 논의하기로 함.

## 로컬 개발 환경
- 서버: `.local-server.ps1` (PowerShell 기반 정적 서버, 8080포트). 가끔 꺼져있어서 재시작 필요할 때 있음.
- 서버 재시작 명령: `.local-server.ps1 -Port 8080`을 `Start-Process powershell`로 백그라운드 실행.
